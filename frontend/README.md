### Visual Adventure

- `npm ci`

- in the directory containing the [.env file template](../env/template.env), create a copy of the file, rename the copy to `.env.development`, and update its properties

- `npm run dev`

- open http://localhost:8000
